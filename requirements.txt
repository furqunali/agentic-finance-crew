# Base runtime — light, no LLM/keys needed. The demo, API and tests all run on this.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Persistence: SQLAlchemy ORM + Alembic migrations. Runs on the bundled SQLite
# out of the box; set DATABASE_URL=postgresql+psycopg://... for Postgres.
SQLAlchemy>=2.0
alembic>=1.13

# Auth: JWT bearer tokens + form parsing for the OAuth2 password login.
# Password hashing uses only the Python stdlib (PBKDF2) — no bcrypt/argon2 wheels.
PyJWT>=2.8
python-multipart>=0.0.9

# PostgreSQL driver (optional) — only needed when DATABASE_URL points at Postgres.
# psycopg[binary]>=3.1

# Real multi-agent engine (optional). Uncomment or `pip install crewai` to enable
# USE_CREWAI=true mode. Kept optional so images and CI stay fast.
# crewai>=0.70
